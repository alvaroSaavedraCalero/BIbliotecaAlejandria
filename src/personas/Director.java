package personas;

public class Director extends Bibliotecario{

    public Director(String nombre, int edad, String password, String rol, Integer id) {
        super(nombre, edad, password, rol, id);
    }

    public Director() {
    }
}
